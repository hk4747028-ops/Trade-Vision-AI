# Trade-Vision-AI
This application is an institutional-grade Technical Analysis Chart Scanner that combines a lightweight Streamlit web interface with an OpenCV (Computer Vision) analytical backend. Instead of parsing traditional time-series data streams, the model handles chart screenshots as raw geometric matrices, translating pixel distributions, edge arrays.
